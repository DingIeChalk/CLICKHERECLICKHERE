export default function handler(req, res) {
  const secret = process.env.SECRETHUXSPIN; // your environment variable

  if (!secret) {
    return res.status(500).json({ error: 'Secret not configured.' });
  }

  res.status(200).json({ secret });
}
